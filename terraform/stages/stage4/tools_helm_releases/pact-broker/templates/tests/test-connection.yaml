apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pact-broker.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "pact-broker.name" . }}
    helm.sh/chart: {{ include "pact-broker.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "pact-broker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
